# Tmall Product Knowledge Sync

Normalize product, SKU, image, video, attribute, selling-point, activity, and FAQ data into ecommerce-domain memory candidates. Keep memory scoped to `domain_scope=ecommerce`, `workflow_scope=tmall`, and `share_policy=isolated`.
