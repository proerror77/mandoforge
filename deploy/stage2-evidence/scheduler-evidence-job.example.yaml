apiVersion: batch/v1
kind: Job
metadata:
  name: mandoforge-scheduler-evidence
  labels:
    app.kubernetes.io/name: mandoforge
    app.kubernetes.io/component: scheduler-evidence
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mandoforge
        app.kubernetes.io/component: scheduler-evidence
    spec:
      restartPolicy: Never
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: scheduler-evidence
          image: mandoforge-api:latest
          imagePullPolicy: IfNotPresent
          command:
            - /app/scripts/scheduler-evidence-gate.sh
          env:
            - name: BASE_URL
              value: http://mandoforge-api:8787
            - name: EVIDENCE_DIR
              value: /evidence/scheduler
            - name: ALLOW_BLOCKED
              value: "1"
            - name: MANDOFORGE_STAGE2_GATE_SUBJECT
              valueFrom:
                secretKeyRef:
                  name: mandoforge-secrets
                  key: MANDOFORGE_SCHEDULER_SUBJECT
            - name: MANDOFORGE_STAGE2_GATE_ROLES
              valueFrom:
                secretKeyRef:
                  name: mandoforge-secrets
                  key: MANDOFORGE_SCHEDULER_ROLES
            - name: MANDOFORGE_SCHEDULER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: mandoforge-secrets
                  key: MANDOFORGE_SCHEDULER_TOKEN
            - name: MANDOFORGE_DEV_ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: mandoforge-secrets
                  key: MANDOFORGE_DEV_ADMIN_TOKEN
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          volumeMounts:
            - name: evidence
              mountPath: /evidence
      volumes:
        - name: evidence
          persistentVolumeClaim:
            claimName: mandoforge-stage2-production-evidence
