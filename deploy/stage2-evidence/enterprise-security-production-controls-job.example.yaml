apiVersion: batch/v1
kind: Job
metadata:
  name: mandoforge-enterprise-security-production-controls
  labels:
    app.kubernetes.io/name: mandoforge
    app.kubernetes.io/component: enterprise-security-production-controls
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mandoforge
        app.kubernetes.io/component: enterprise-security-production-controls
    spec:
      restartPolicy: Never
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: enterprise-security-production-controls
          image: mandoforge-api:latest
          imagePullPolicy: IfNotPresent
          command:
            - /app/scripts/enterprise-security-production-controls-gate.sh
          envFrom:
            - secretRef:
                name: mandoforge-stage2-controller-env
          env:
            - name: EVIDENCE_DIR
              value: /evidence/enterprise-security-production-controls-gate
            - name: SOURCE_EVIDENCE_DIR
              value: /evidence/enterprise-security-production-controls
            - name: ENTERPRISE_SECURITY_CONTROLS_EVIDENCE_FILE
              value: /evidence/enterprise-security-production-controls/summary.json
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          volumeMounts:
            - name: evidence
              mountPath: /evidence
      volumes:
        - name: evidence
          persistentVolumeClaim:
            claimName: mandoforge-stage2-production-evidence
