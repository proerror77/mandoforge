apiVersion: apps/v1
kind: Deployment
metadata:
  name: mandoforge-worker-isolated
  labels:
    app: mandoforge-worker-isolated
    mandoforge.io/worker-pool: isolated
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mandoforge-worker-isolated
      mandoforge.io/worker-pool: isolated
  template:
    metadata:
      labels:
        app: mandoforge-worker-isolated
        mandoforge.io/worker-pool: isolated
    spec:
      serviceAccountName: mandoforge-worker
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: worker
          image: ghcr.io/proerror77/mandoforge/mandoforge-api:latest
          imagePullPolicy: IfNotPresent
          command: ["mandoforge-api"]
          envFrom:
            - configMapRef:
                name: mandoforge-config
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: mandoforge-worker-secrets
                  key: DATABASE_URL
            - name: MANDOFORGE_WORKER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: mandoforge-worker-secrets
                  key: MANDOFORGE_WORKER_TOKEN
            - name: MANDOFORGE_PROVIDER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: mandoforge-worker-secrets
                  key: MANDOFORGE_PROVIDER_API_KEY
                  optional: true
            - name: DEEPSEEK_API_KEY
              valueFrom:
                secretKeyRef:
                  name: mandoforge-worker-secrets
                  key: DEEPSEEK_API_KEY
                  optional: true
            - name: MANDOFORGE_VAULT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: mandoforge-worker-secrets
                  key: MANDOFORGE_VAULT_TOKEN
                  optional: true
            - name: MANDOFORGE_PROCESS_ROLE
              value: "worker"
            - name: MANDOFORGE_SERVICE_NAME
              value: "mandoforge-worker-isolated"
            - name: WORKER_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: WORKER_POOL
              value: isolated
            - name: MANDOFORGE_REMOTE_COMPUTER_ASSIGNMENT_MODE
              value: required
            - name: POLL_INTERVAL_SECONDS
              value: "2"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: workspaces
              mountPath: /var/lib/mandoforge/workspaces
      volumes:
        - name: tmp
          emptyDir: {}
        - name: workspaces
          persistentVolumeClaim:
            claimName: mandoforge-workspaces
