# Domain Docs

This is a single-context repository.

Before exploring domain behavior, read root `CONTEXT.md` and relevant ADRs under `docs/adr/` when they exist. If either path is absent, proceed without creating placeholder documentation.

Use terminology defined in `CONTEXT.md`. Surface conflicts with an existing ADR instead of silently overriding it.
