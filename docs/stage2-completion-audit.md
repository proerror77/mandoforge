# Stage 2 Completion Audit

Audit date: 2026-05-14

Objective: complete Stage 2, "Governed Runtime Pilot", for the Rust-native Generic Agent OS runtime.

This audit is intentionally strict. A reserved boundary, UI placeholder, passing unit test, or summary dashboard counts only when it maps to a concrete Stage 2 requirement. Items marked Partial or Gap must be closed before Stage 2 can be declared complete.

## Success Criteria And Evidence

| Requirement | Evidence | Status |
| --- | --- | --- |
| Multi-tenancy with org/team/project scopes | `organizations`, `teams`, `projects`, `memberships`, and `tenant_invitations` exist; incoming `x-mandoforge-tenant-id` must match the runtime tenant and fails closed otherwise; `db/migrations/0024_tenant_rls_policies.sql` enables and forces RLS for tracked tenant-scoped tables plus indirect `agent_versions` isolation; Postgres connections set `mandoforge.tenant_id` to the configured runtime tenant; `GET /api/tenant-isolation/readiness` reports the single-runtime-tenant boundary, header fail-closed status, scoped resource counts, tenant-scoped table coverage, RLS migration asset state, tenant context state, enabled/forced table counts, a production routing gate, and latest optional external routing-controller evidence. `POST /api/tenant-isolation/routing/validate` can execute an external validation controller through `MANDOFORGE_TENANT_ROUTING_CONTROLLER_URL`; `MANDOFORGE_TENANT_ROUTING_CONTROLLER_REQUIRED=true` makes production routing readiness fail closed until configured controller evidence is validated. `scripts/tenant-isolation-evidence-gate.sh` and `deploy/stage2-evidence/tenant-isolation-evidence-job.example.yaml` provide a focused tenant isolation evidence path for readiness plus routing validation under the Stage 2 production evidence PVC. Scoped agent/session access and routing evidence are tested by `admin_can_manage_stage2_governance_scope`, `tenant_provisioning_bootstrap_creates_owner_scope_and_audit`, `tenant_invitations_create_accept_revoke_and_audit_membership`, `request_tenant_header_must_match_runtime_tenant`, `tenant_production_routing_controller_executes_external_boundary`, and `tenant_production_routing_readiness_requires_controller_when_configured`. | Partial: runtime still serves one configured tenant, but production cross-tenant request routing is now explicitly gate-blocked and independently evidence-collectable until runtime tenant routing, full RLS readiness, and optional required controller evidence exist. |
| RBAC for admin/operator/approver/viewer | `authorization.rs` defines roles and permissions; read/write/run/tool/approval/job/admin routes enforce them; tests include `read_routes_enforce_rbac_role_header`, `write_routes_enforce_rbac_role_header`, `session_run_enforces_rbac_role`, and `approval_decision_enforces_rbac_role`. | Covered |
| Provider governance and model allowlists | Provider registry, audited provider update, team provider access create/update/archive, model allowlists, provider health, lifecycle approvals, emergency status changes, credential refs, provider API key ref rotation, budget enforcement, governance summary, audited provider deployment validation, provider policy gate report, manual and scheduler-driven audited provider gate run/history surfaces, production enforcement status for failed/warning/stale/missing provider gates, an audited production rollout run that blocks unless the latest gate is fresh, passing, covers the current provider set, and an external rollout controller confirms the apply, plus an audited production rollback run that requires prior applied-rollout evidence and an external rollback/recovery controller. Deployment validation can execute an external controller through `MANDOFORGE_PROVIDER_DEPLOYMENT_CONTROLLER_URL`; `MANDOFORGE_PROVIDER_DEPLOYMENT_CONTROLLER_REQUIRED=true` makes readiness fail closed unless recent validated controller evidence exists. The rollout controller boundary is configured through `MANDOFORGE_PROVIDER_ROLLOUT_CONTROLLER_URL`, sends only provider metadata and gate evidence, records controller status/steps/deployment id in audit, and fails closed when missing or unhealthy. The rollback controller boundary is configured through `MANDOFORGE_PROVIDER_ROLLOUT_ROLLBACK_CONTROLLER_URL`, sends only provider ids and source rollout evidence, records controller status/steps/rollback id in audit, and fails closed when missing, unhealthy, or when no applied rollout exists. `scripts/provider-governance-evidence-gate.sh` and `deploy/stage2-evidence/provider-governance-evidence-job.example.yaml` provide a focused provider evidence path for summary, policy gate, deployment validation, and optional rollout/rollback proof under the Stage 2 production evidence PVC. Static UI now exposes the provider API key ref rotation form backed by the audited rotation API. Tests include `admin_can_manage_stage2_governance_scope` for provider access allowlist update/archive enforcement and audit, `provider_health_resolves_vault_api_key_ref_for_external_probe`, `provider_status_approval_requires_separate_approver_and_audits_decision`, `provider_summary_aggregates_governance_signals`, `provider_deployment_controller_executes_external_boundary`, `provider_deployment_readiness_requires_controller_when_configured`, `provider_production_rollout_requires_fresh_passing_gate`, `provider_production_rollout_executes_external_controller`, `provider_production_rollback_executes_external_controller`, and `provider_production_rollback_blocks_without_applied_rollout`. | Partial: provider access, provider credential rotation, and provider lifecycle governance now have audited API/UI-visible wiring, and provider deployment, production rollout, and production rollback are fail-closed and external-controller-backed, but full production rollout remains incomplete until these controller-backed operations are exercised against a real deployment target. |
| Vault and secret references | Secret ref catalog, Vault KV v2 read/write client boundary, optional fail-closed secret value write on catalog create/rotate, rotation records, provider secret resolution, MCP secret refs, eval judge secret refs, `GET /api/vault/readiness`, production rotation readiness, audited `POST /api/vault/kms/rotation/run`, production recovery readiness, and audited `POST /api/vault/kms/recovery/validate` production-readiness gates exist. The readiness gate checks Vault provider health, reserved KMS configuration, required KMS validation endpoint, registered refs, provider/MCP/eval judge consumers, unresolved refs, stale rotations, recent validated rotation-run evidence, and optional required KMS recovery-controller evidence through `MANDOFORGE_KMS_RECOVERY_CONTROLLER_URL`; `MANDOFORGE_KMS_RECOVERY_CONTROLLER_REQUIRED=true` makes recovery readiness fail closed unless validated controller drill evidence exists. The KMS rotation run performs a bounded HTTP call to `MANDOFORGE_KMS_ENDPOINT`, records external execution status/counts without secret values, and updates local catalog version metadata only when the external KMS response confirms rotated secret record ids; the recovery validation sends secret metadata only, never secret values. `scripts/vault-evidence-gate.sh` and `deploy/stage2-evidence/vault-evidence-job.example.yaml` provide a focused Vault/KMS evidence path for readiness, health, recovery validation, and optional KMS rotation under the Stage 2 production evidence PVC. The static Vault panel renders rotation and recovery gates. Tests include `vault_secret_provider_reads_kv_v2_secret_over_http`, `vault_secret_provider_writes_kv_v2_secret_over_http`, `secret_refs_reject_absolute_or_parent_paths`, `eval_judge_profile_api_normalizes_secret_ref_and_audits`, `vault_readiness_reports_secret_consumers_and_kms_gate`, `vault_kms_rotation_executes_external_endpoint_and_updates_catalog_metadata`, and `vault_kms_recovery_controller_executes_external_boundary`. | Partial: Vault KV v2 value write, external KMS rotation execution, external recovery-drill validation boundaries, and a dedicated evidence gate/Job are implemented, but full production secret lifecycle operations remain incomplete until real KMS/HSM adapters, envelope encryption policy, approval-backed secret value rotation, and recovery/rollback procedures are validated in a production cluster. |
| Worker queue, leases, retries, and resume | In-memory/Postgres execution queue, Rust `mandoforge-worker` binary, shell worker loop, K8s worker Deployment, restricted worker ServiceAccount, worker NetworkPolicy, worker HPA skeleton, worker KEDA queue-depth ScaledObject, Redis Streams backend, Core NATS backend, NATS JetStream durable handoff, lease/retry behavior, stale running reclaim, queue-backed Codex App Server execution, Remote Computer manual and automatic worker lease assignment, assignment lifecycle updates, `GET /api/execution-jobs/worker-readiness`, audited `POST /api/execution-jobs/worker-load-validation/run` evidence, optional external worker load-validation controller execution through `MANDOFORGE_WORKER_LOAD_VALIDATION_CONTROLLER_URL`, and worker production-ops readiness are implemented. `MANDOFORGE_WORKER_LOAD_VALIDATION_CONTROLLER_REQUIRED=true` makes production load validation fail closed until configured controller evidence is validated. The readiness gate reports queue semantics, worker mode, queued/running/retryable/failed pressure, stale leases, K8s worker manifest coverage, worker Pod hardening status, token automount, seccomp, privilege/capability/read-only-root checks, resource bounds, NetworkPolicy presence, HPA/KEDA autoscaling manifest presence, parsed scale targets/min/max replicas, trigger types, queue-depth scaling evidence, validation status, latest load-validation evidence, latest controller evidence, isolated worker-pool gate state, production-ops blockers, and runbook actions; `scripts/worker-evidence-gate.sh` and `deploy/stage2-evidence/worker-evidence-job.example.yaml` provide a focused worker evidence path for readiness and load validation under the Stage 2 production evidence PVC; the static Worker Dashboard renders it. Tests include `execution_queue_tracks_job_lifecycle`, `queue_backed_worker_defers_approved_tool_until_job_run`, `queue_backed_worker_auto_assigns_active_remote_computer_lease`, `worker_load_validation_controller_executes_external_boundary`, `worker_load_validation_evidence_requires_controller_when_configured`, Redis broker tests, Core NATS broker publish/message parsing tests, JetStream command/publish/drain/ack tests, and worker readiness/load-validation hardening assertions. `cargo check -p mandoforge-api --bin mandoforge-worker` passes. | Partial: worker K8s hardening is now manifest-backed and API/UI-visible, and load-validation evidence plus production-ops blockers are auditable; optional required external controller evidence now prevents env-only validation claims, and a dedicated evidence gate/Job makes worker proof independently collectable, but actual production load-validated autoscaling and isolated worker-pool operation remain incomplete until the controller-backed gate is exercised against a real cluster profile. |
| Agent Remote Computer substrate | K8s readiness skeleton exists for a Pod-based remote computer direction: `deploy/k8s/agent-remote-computer.yaml`, service account, RWX state PVC placeholder, mounted state-contract ConfigMap for Memory/Notes/Skills/artifacts/locks, JuiceFS CSI profile manifest, production state-sync readiness, audited `POST /api/remote-computers/state-sync/validate`, optional external state-sync controller execution through `MANDOFORGE_REMOTE_COMPUTER_STATE_SYNC_CONTROLLER_URL`, `MANDOFORGE_REMOTE_COMPUTER_STATE_SYNC_CONTROLLER_REQUIRED=true` fail-closed state-sync enforcement, warm-pool Deployment example, KEDA ScaledObject example, fail-closed artifact discovery sidecar ConfigMap/container, opt-in `deploy/kustomization.yaml` pilot bundle, deny-by-default NetworkPolicy, `GET /api/remote-computers/readiness`, static UI readiness panel, `remote_computers` / `remote_computer_leases` storage, lease heartbeat/release/fail APIs, `remote_computer_session_attachments`, `remote_computer_job_assignments`, persisted `remote_computer_state_locks`, persisted `remote_computer_sidecar_heartbeats`, state lock acquire/release APIs with event/audit records, sidecar heartbeat API with event/audit records, missing/stale sidecar readiness alerts, scheduler due-plan/run supervision for unhealthy sidecars, audited `POST /api/remote-computers/sidecars/recovery/run` replacement planning with optional `MANDOFORGE_REMOTE_COMPUTER_SIDECAR_VALIDATION_URL` post-replacement validation, `MANDOFORGE_REMOTE_COMPUTER_SIDECAR_VALIDATION_REQUIRED=true` fail-closed validation enforcement, manual and automatic worker assignment to active matching Remote Computer leases, automatic warm-pool lease claims from available Remote Computers, assignment completed/released/failed/canceled lifecycle events, worker acknowledgement, reserved Pod exec transport planning, Pod exec dry-run intent, gated Kubernetes exec WebSocket capture with timeout/output bounds in the runner boundary, env-gated approved `file.write`, `shell.exec`, and `codex.exec` execution through an assigned Remote Computer Pod, execution-job cancellation through assigned Pod deletion, Codex final-message artifact capture from Pod exec output, Kubernetes live-create payload sidecar env injection, push-based Remote Computer artifact sync, bounded shared-workspace artifact discovery through `POST /api/remote-computers/artifacts/discover`, execution transport readiness with assignment counts and required implementation steps, stale attachment listing, Admin-only stale reclaim, scheduler due-plan/run integration for stale reclaim, a reserved `RemoteComputerRunner` boundary, explicit `KubernetesRemoteComputerRunner` skeleton, Admin-only `GET /api/remote-computers/runner/readiness`, Admin-only audited `POST /api/remote-computers/runner/dry-run`, Admin-only audited `POST /api/remote-computers/runner/mutate`, gated Kubernetes Pod create/delete HTTP calls, a focused `scripts/remote-computer-evidence-gate.sh` plus `deploy/stage2-evidence/remote-computer-evidence-job.example.yaml` for state-sync/runner/sidecar evidence collection into the Stage 2 production evidence PVC, UI runner/attachment/handoff/state-sync/sidecar heartbeat/recovery evidence, and `remote_computer.*` session event/audit records. Tests include `remote_computer_state_sync_controller_executes_external_boundary`, `remote_computer_state_sync_readiness_requires_controller_when_configured`, `remote_computer_sidecar_recovery_requires_validation_controller_when_configured`, `remote_computer_sidecar_validation_controller_confirms_replacement`, and sidecar heartbeat/recovery tests. | Partial: normal worker drains can now create and finalize handoff assignments from existing leases or claim an available warm-pool Remote Computer, and approved `file.write`/`shell.exec`/`codex.exec`, execution-job cancellation, push-based artifact sync, bounded shared-workspace artifact discovery, fail-closed sidecar manifesting, live-create sidecar env injection, sidecar heartbeats, sidecar stale/missing detection, scheduler-supervised sidecar audit runs, env-gated sidecar replacement planning, optional or required validation-controller confirmation, control-plane state locks, optional or required production state-sync controller validation, and a dedicated evidence gate/Job are routed through assigned Pod control-plane surfaces when gates are enabled, but production pod replacement and distributed Memory/Notes/Skills state sync still need the required controllers exercised against a real cluster; JuiceFS profile, warm pool, and KEDA are bundled as opt-in pilot manifests but not production-hardened. |
| Approval v2: modify, delegate, expire, groups, escalation, delivery | Approval modification, expiry, delegated approver, approval groups, escalation rules, due escalation, webhook/Slack/email-relay notification delivery, `GET /api/approvals/notification-routing/summary`, bounded `POST /api/approvals/notifications/run`, audited `POST /api/approvals/notifications/deployment/validate`, audited `POST /api/approvals/notifications/ops/validate`, and `GET /api/approvals/notifications/runs` are implemented. The routing summary reports configured approval notification channels, persisted channel policy counts, retry/backoff policy, pending/delegated/group-routed approval counts, routable versus unroutable pending approvals, group/rule coverage, and attention items; the delivery run skips recently notified approvals and writes audit history; the deployment validation gate checks channel/policy/routability evidence without sending notifications and can execute an external deployment controller through `MANDOFORGE_APPROVAL_NOTIFICATION_DEPLOYMENT_CONTROLLER_URL`; `MANDOFORGE_APPROVAL_NOTIFICATION_DEPLOYMENT_CONTROLLER_REQUIRED=true` makes deployment readiness fail closed unless recent validated controller evidence exists. Production notification ops can execute an external ops controller through `MANDOFORGE_APPROVAL_NOTIFICATION_OPS_CONTROLLER_URL`; `MANDOFORGE_APPROVAL_NOTIFICATION_OPS_CONTROLLER_REQUIRED=true` makes production ops readiness fail closed unless recent ops-controller evidence is validated. `scripts/approval-notification-evidence-gate.sh` and `deploy/stage2-evidence/approval-notification-evidence-job.example.yaml` provide a focused approval notification evidence path for routing, run history, deployment validation, ops validation, and optional delivery evidence under the Stage 2 production evidence PVC. The static Approval Governance panel renders routing, policy archive actions, run history, production-ops readiness, deployment-readiness, latest validation evidence, and controller status. Tests include `approval_modify_updates_waiting_tool_args_before_approve`, `delegated_approval_requires_matching_subject_or_admin`, `approval_groups_and_escalation_rules_delegate_decisions`, `due_approval_escalation_run_advances_pending_approvals_by_rule_order`, `approval_notification_routing_summary_reports_routable_pending_work`, `approval_notification_deployment_controller_executes_external_boundary`, `approval_notification_deployment_readiness_requires_controller_when_configured`, `approval_notification_ops_controller_executes_external_boundary`, `approval_notification_production_ops_requires_controller_when_configured`, `approval_notification_channel_policy_is_persisted_and_fail_closed_without_endpoint`, and `approval_notification_channel_policy_retries_transient_delivery_failure`. | Partial: production multi-channel delivery now has webhook/Slack/email-relay boundaries, persisted notification channel policy, bounded retries/backoff, audited run history, production-ops readiness, deployment and ops validation gates, optional required external deployment/ops controller boundaries, and a dedicated evidence gate/Job, but provider-grade notification operations remain incomplete until these gates are exercised against real notification providers. |
| MCP Gateway with registry, allowlists, policy, secret refs, health, rollout | `mcp.call`, team MCP server registry, discovery import, secret refs, health checks, due health, deployment validation, rollout request/apply/due/rollback, rollout run history, production rollout readiness, production orchestration readiness, deployment readiness, optional external deployment controller execution through `MANDOFORGE_MCP_DEPLOYMENT_CONTROLLER_URL`, optional external rollout controller execution through `MANDOFORGE_MCP_ROLLOUT_CONTROLLER_URL`, optional external rollback/recovery controller execution through `MANDOFORGE_MCP_ROLLOUT_ROLLBACK_CONTROLLER_URL`, and summary UI exist. `MANDOFORGE_MCP_DEPLOYMENT_CONTROLLER_REQUIRED=true` makes deployment readiness fail closed unless recent validated controller evidence exists; `MANDOFORGE_MCP_ROLLOUT_CONTROLLER_REQUIRED=true` makes due rollouts skip automatic apply when the external controller is missing; `MANDOFORGE_MCP_ROLLOUT_ROLLBACK_CONTROLLER_REQUIRED=true` makes rollback fail closed unless the rollback controller confirms recovery. `scripts/mcp-gateway-evidence-gate.sh` and `deploy/stage2-evidence/mcp-gateway-evidence-job.example.yaml` provide a focused MCP evidence path for team-scoped rollout summary, run history, deployment validation, optional due-run supervision, and `team-discovery.json` under the Stage 2 production evidence PVC; the focused MCP gate and strict production evidence gate now auto-discover the first active team through `/api/organizations` and `/api/organizations/{id}/teams` when `MANDOFORGE_STAGE2_TEAM_ID` is empty, and the completion audit gate reuses that discovery artifact before marking team-scoped endpoints unresolved. Tests include `mcp_call_executes_through_tool_router_and_gateway_policy`, `mcp_server_config_normalizes_secret_refs_without_secret_values`, `mcp_deployment_controller_executes_external_boundary`, `mcp_deployment_readiness_requires_controller_when_configured`, `mcp_rollout_required_controller_skips_due_apply_when_missing`, `mcp_rollout_rollback_controller_executes_external_boundary`, and `mcp_rollout_rollback_controller_required_flag_is_fail_closed`. | Partial: connector rollout now has audited due-run history plus fail-closed production rollout/orchestration/deployment-validation gates, required deployment/rollout/rollback controller boundaries, a dedicated evidence gate/Job, automatic evidence-team discovery, and manual rollback with controller evidence, but production connector orchestration is still limited beyond summary, due-run, manual apply, rollback, and validation evidence until real deployment supervision is validated. |
| Codex App Server adapter | Env-gated health/deployment-validation/ops-validation/thread/turn/command/interrupt/poll/stale-poll/artifact-sync/trace/control-summary routes exist; approved `codex.exec` supports CLI/App Server strategies and queue-backed polling; the control-plane summary now reports production-ops readiness that blocks on missing config, failed/interrupted traces, stale non-terminal turns, missing/stale stale-poll supervision, failed stale-poll due-runs, or missing required ops-controller evidence, plus deployment readiness that blocks on missing/stale/unhealthy deployment validation evidence or missing required deployment-controller evidence. The static Codex panel now renders per-turn trace status and operation breakdowns, command IDs, trace detail status/operation tables, errors, and status timelines instead of relying only on raw JSON. Deployment validation can execute an external controller through `MANDOFORGE_CODEX_APP_SERVER_DEPLOYMENT_CONTROLLER_URL`; `MANDOFORGE_CODEX_APP_SERVER_DEPLOYMENT_CONTROLLER_REQUIRED=true` makes deployment readiness fail closed unless recent validated controller evidence exists. Production ops validation can execute an external controller through `MANDOFORGE_CODEX_APP_SERVER_OPS_CONTROLLER_URL`; `MANDOFORGE_CODEX_APP_SERVER_OPS_CONTROLLER_REQUIRED=true` makes production ops readiness fail closed unless recent validated ops-controller evidence exists. `scripts/codex-app-server-evidence-gate.sh` and `deploy/stage2-evidence/codex-app-server-evidence-job.example.yaml` provide a focused Codex App Server evidence path for health, control-plane summary, run/trace inventory, deployment validation, ops validation, and optional stale-poll evidence under the Stage 2 production evidence PVC. Tests include `codex_app_server_routes_require_admin_and_call_adapter`, `codex_app_server_production_ops_requires_fresh_stale_poll_supervision`, `codex_app_server_ops_controller_executes_external_boundary`, `codex_app_server_production_ops_requires_controller_when_configured`, `codex_app_server_deployment_controller_executes_external_boundary`, `codex_app_server_deployment_readiness_requires_controller_when_configured`, `approved_codex_exec_can_use_app_server_strategy`, and queue-backed polling tests. | Partial: production App Server operations now have fail-closed production-ops, deployment-validation and ops-validation gates, optional required external deployment/ops controller evidence, richer trace dashboards, and a dedicated evidence gate/Job, but operations beyond steering, polling, traces, artifact sync, summary, stale-poll supervision, and controller-backed validation still need real deployment supervision. |
| Evaluation, regression gates, release approvals, rollback, drift | Eval datasets/cases/runs, deterministic Stage 2 graders, optional judge client/profile, Stage 2 regression suite, gates, release requests, approval automation, due run, optional external release controller execution through `MANDOFORGE_AGENT_RELEASE_CONTROLLER_URL`, audited orchestration validation through `POST /api/agents/releases/orchestration/validate`, optional external orchestration controller execution through `MANDOFORGE_AGENT_RELEASE_ORCHESTRATION_CONTROLLER_URL`, audited deployment validation through `POST /api/agents/releases/deployment/validate`, optional external deployment controller execution through `MANDOFORGE_AGENT_RELEASE_DEPLOYMENT_CONTROLLER_URL`, release automation run history, release production rollout readiness, release production orchestration readiness, release deployment readiness, rollback, optional external rollback/recovery controller execution through `MANDOFORGE_AGENT_RELEASE_ROLLBACK_CONTROLLER_URL`, and drift checks exist. `MANDOFORGE_AGENT_RELEASE_CONTROLLER_REQUIRED=true` makes due automation skip eligible auto-promotions when the external rollout controller is missing; `MANDOFORGE_AGENT_RELEASE_ORCHESTRATION_CONTROLLER_REQUIRED=true` makes production orchestration readiness fail closed unless recent validated orchestration-controller evidence exists; `MANDOFORGE_AGENT_RELEASE_DEPLOYMENT_CONTROLLER_REQUIRED=true` makes deployment readiness fail closed unless recent validated controller evidence exists; `MANDOFORGE_AGENT_RELEASE_ROLLBACK_CONTROLLER_REQUIRED=true` makes rollback fail closed unless the external rollback/recovery controller is configured and confirms the operation. Controller executions send release/eval/automation metadata only, skip failed controller responses instead of promoting, validating, or rolling back, and expose controller counts/evidence in API/UI. `scripts/eval-release-evidence-gate.sh` and `deploy/stage2-evidence/eval-release-evidence-job.example.yaml` provide a focused eval/release evidence path for release summary, automation history, deployment validation, orchestration validation, and optional Stage 2 regression/due-run evidence under the Stage 2 production evidence PVC. Tests include `stage2_eval_suite_bootstrap_creates_passing_regression_cases_and_audits`, `agent_release_controller_executes_external_rollout_boundary`, `agent_release_required_controller_skips_auto_promotion_when_missing`, `agent_release_orchestration_controller_executes_external_boundary`, `agent_release_orchestration_readiness_requires_controller_when_configured`, `agent_release_deployment_controller_executes_external_boundary`, `agent_release_deployment_readiness_requires_controller_when_configured`, `agent_release_rollback_controller_executes_external_boundary`, `agent_release_rollback_controller_required_flag_is_fail_closed`, and eval judge/gate tests. | Partial: release automation now has audited run history, external rollout/orchestration/deployment/rollback controller boundaries, required-controller fail-closed behavior, fail-closed production rollout/orchestration/deployment-validation gates, and a dedicated evidence gate/Job, but production multi-step release rollout orchestration is still not complete until these controller-backed operations are exercised against a real production deployment target. |
| Observability with OTel traces/metrics/logs | OTLP-shaped logs/traces/metrics export from event append boundary, observability summary, collector readiness, production-ops gate, audited collector deployment-validation gate, optional external collector deployment controller execution through `MANDOFORGE_OBSERVABILITY_COLLECTOR_DEPLOYMENT_CONTROLLER_URL`, audited collector cluster rollout validation through `POST /api/observability/collector/cluster/validate`, optional external collector cluster controller execution through `MANDOFORGE_OBSERVABILITY_COLLECTOR_CLUSTER_CONTROLLER_URL`, remediation supervision readiness, remediation plan/run, optional external remediation controller execution through `MANDOFORGE_OBSERVABILITY_REMEDIATION_CONTROLLER_URL`, scheduler due-plan, and scheduler orchestration summary exist. `deploy/k8s/otel-collector.yaml` now provides an in-cluster OTel Collector target with OTLP HTTP on `4318`, health on `13133`, hardened Pod settings, API config for separate `MANDOFORGE_OTEL_EXPORTER_OTLP_ENDPOINT` / `MANDOFORGE_OTEL_COLLECTOR_HEALTH_ENDPOINT`, and `deploy/k8s/otel-collector-networkpolicy.yaml` limits collector ingress to the API Pod; `scripts/verify-observability-k8s-manifests.sh` checks the rendered collector bundle. `scripts/observability-collector-evidence-gate.sh` and `deploy/stage2-evidence/observability-collector-evidence-job.example.yaml` provide a focused collector rollout proof path that records readiness, deployment validation, cluster rollout validation, optional remediation, and summary evidence into the Stage 2 production evidence PVC. `MANDOFORGE_OBSERVABILITY_COLLECTOR_DEPLOYMENT_CONTROLLER_REQUIRED=true`, `MANDOFORGE_OBSERVABILITY_COLLECTOR_CLUSTER_CONTROLLER_REQUIRED=true`, and `MANDOFORGE_OBSERVABILITY_REMEDIATION_CONTROLLER_REQUIRED=true` make production readiness fail closed until configured controller execution evidence exists. Tests include `appended_session_events_export_telemetry_when_enabled`, `http_telemetry_exporter_posts_logs_traces_and_metrics_to_otlp_boundary`, `observability_summary_reports_dashboard_backpressure`, `observability_collector_deployment_controller_executes_external_boundary`, `observability_collector_deployment_readiness_requires_controller_when_configured`, `observability_collector_cluster_controller_executes_external_boundary`, `observability_collector_cluster_readiness_requires_controller_when_configured`, `observability_remediation_controller_executes_external_boundary`, and `observability_remediation_supervision_blocks_when_required_without_controller_evidence`. | Partial: collector production ops, deployment validation, cluster rollout validation, and an in-cluster collector deployment skeleton are now API/UI/K8s-visible and fail closed; a dedicated evidence gate/Job now makes collector rollout evidence independently collectable and archivable, but this is still not proof of a real collector rollout until the controller-backed Job is exercised against a production cluster. |
| Cost tracking, budgets, alerts, trends, finance exports | Usage summary/trends, provider cost budgets, alert routes/delivery/ack, finance summary, CSV export, scheduled export delivery, scheduler-driven current cost alert delivery, alert-delivery audit metadata, Finance Operations readiness/runbook UI panels, production close readiness gate, a controlled finance close run endpoint, optional external close controller execution through `MANDOFORGE_FINANCE_CLOSE_CONTROLLER_URL`, and a bounded accounting reconciliation controller endpoint through `POST /api/usage/finance-operations/reconcile` exist. `MANDOFORGE_FINANCE_CLOSE_CONTROLLER_REQUIRED=true` makes production close readiness fail closed until configured close-controller evidence is closed; `MANDOFORGE_FINANCE_RECONCILIATION_CONTROLLER_REQUIRED=true` makes production close readiness fail closed until configured reconciliation-controller evidence is reconciled. `scripts/finance-evidence-gate.sh` and `deploy/stage2-evidence/finance-evidence-job.example.yaml` provide a focused finance evidence path for dashboard summary, operations readiness, and optional close/reconciliation controller evidence under the Stage 2 production evidence PVC. Tests include `builds_usage_trend_from_rollups_and_budget_pressure`, `builds_usage_finance_dashboard_attention_items`, `builds_usage_finance_operations_summary_from_audit_history`, `usage_finance_summary_requires_admin_and_reports_dashboard`, `finance_close_controller_executes_external_boundary`, `finance_production_close_readiness_requires_controller_when_configured`, `finance_reconciliation_controller_executes_external_boundary`, `finance_production_close_readiness_requires_reconciliation_when_configured`, and `scheduler_due_run_orchestrates_due_automation_across_teams`. | Partial: production finance operations now include readiness, audit-evidence summaries, scheduler current-alert delivery, a bounded close run, a fail-closed production close gate, external close and accounting reconciliation controller boundaries, optional required-controller enforcement, and a dedicated evidence gate/Job, but full production finance close workflows are not complete until that reconciliation is exercised against a real accounting system. |
| UI v2 admin console | Static UI covers tenant governance, provider settings, Vault, approval governance, policy, eval/release, MCP, worker, usage, Codex App Server, observability, remediation, scheduler orchestration, and Remote Computer readiness/state-lock/artifact-discovery/sidecar-recovery panels. Tenant governance now exposes create/update/archive/delete organization, create/update/archive/delete team, create/update/archive/delete project, create/delete membership, create/revoke/accept invitation, bootstrap tenant, and transfer ownership actions against the governance APIs; membership deletion is audited and blocks deletion of the last org admin membership, while team/project deletion requires archived state and no child references. Provider settings now expose create/update/status/approval/health/gate/deployment/rollout/rollback operations against the governance APIs. MCP settings now expose explicit update and rollout request forms, removing prompt-driven MCP config mutation from the admin path while retaining apply/rollback/status/health/discovery controls. The policy panel now renders rollout orchestration readiness and can trigger `POST /api/policy/rollout/orchestration/validate` with optional external controller evidence through `MANDOFORGE_POLICY_ROLLOUT_ORCHESTRATION_CONTROLLER_URL`; `MANDOFORGE_POLICY_ROLLOUT_ORCHESTRATION_CONTROLLER_REQUIRED=true` makes readiness fail closed without validated controller evidence. `scripts/policy-rollout-evidence-gate.sh` and `deploy/stage2-evidence/policy-rollout-evidence-job.example.yaml` provide a focused policy rollout evidence path for orchestration readiness, validation, and optional due-run evidence under the Stage 2 production evidence PVC. The Codex App Server panel includes structured per-turn trace drill-downs with duration, next action, command IDs, latest error, terminal/non-terminal counts, and status timelines. `GET /api/stage2/readiness` returns a fail-closed Stage 2 completion gate plus 12 `evidence_requirements` that map each open completion gap to production targets, readiness endpoints, validation endpoints, and required evidence. The Governance panel renders the Stage 2 completion gate, open gaps, and Stage 2 Evidence Checklist. `./scripts/verify-static-ui-actionbook.sh` validates a static UI smoke path without relying on browser MCP and now checks the Stage 2 evidence checklist plus tenant scope update/delete, provider update/access, MCP update/rollout forms without `window.prompt`, tenant membership delete, and invitation accept wiring; `scripts/stage2-production-evidence-gate.sh` can optionally run that Actionbook smoke with `RUN_STAGE2_UI_ACTIONBOOK=1`, and `scripts/stage2-completion-audit-gate.sh` maps local script evidence artifacts instead of treating them as unresolved endpoints. Tests include `admin_can_archive_tenant_lifecycle_scopes`, `provider_summary_aggregates_governance_signals`, `stage2_readiness_parses_open_completion_gaps`, `policy_rollout_orchestration_controller_executes_external_boundary`, and `policy_rollout_orchestration_readiness_requires_controller_when_configured`. | Partial: full production CRUD flows, dashboard polish, and richer rollout operations remain incomplete, but policy rollout orchestration, Stage 2 completion evidence, tenant scope update/delete, provider update/access, MCP update/rollout, tenant membership deletion, tenant invitation accept/revoke operations, and optional static UI evidence archiving now have API/audit/UI-visible wiring instead of only partial backend surfaces. |
| Kubernetes scheduler/deployment groundwork | `deploy/k8s` kustomize renders successfully, including scheduler CronJob for aggregate due-run supervision, a dedicated scheduler ServiceAccount with token automount disabled, Secret-sourced scheduler identity/token headers, shared-token enforcement for `POST /api/scheduler/run-due` when `MANDOFORGE_SCHEDULER_TOKEN` is configured, scheduler deployment readiness in `GET /api/scheduler/summary`, a dedicated `scripts/scheduler-evidence-gate.sh` that captures scheduler summary, due-plan, shared-token-authenticated run-due evidence, and a focused scheduler evidence Job that writes to the Stage 2 production evidence PVC, plus hardened worker ServiceAccount/NetworkPolicy manifests. The runtime image now includes `bash`, `curl`, `jq`, and `scripts/` so `scripts/stage2-production-evidence-gate.sh` and focused evidence gates can run in cluster; the strict production evidence gate also collects scheduler due-plan evidence and uses the scheduler shared-token header when validation mode executes `POST /api/scheduler/run-due`. `deploy/stage2-evidence` renders an opt-in Stage 2 evidence Job that calls the in-cluster API, writes evidence to an ephemeral volume, and defaults to `ALLOW_BLOCKED=1` so it inventories incomplete deployments without claiming completion. `scripts/render-stage2-controller-secret.sh` renders the strict controller Secret from a real env file and fails closed on placeholder URLs or empty token/key values by default while allowing `MANDOFORGE_STAGE2_TEAM_ID` to stay empty for MCP team auto-discovery. `deploy/stage2-production-evidence` renders the strict production evidence PVC/Job bundle, including a persistent evidence PVC mount for `/evidence`, a strict production evidence Job, a completion audit Job that writes `/evidence/completion-audit/checklist.md` and `/evidence/completion-audit/checklist.json`, a focused observability collector evidence Job, a focused Remote Computer evidence Job, a focused provider governance evidence Job, a focused tenant isolation evidence Job, a focused Vault/KMS evidence Job, a focused approval notification evidence Job, a focused worker evidence Job, a focused scheduler evidence Job, a focused policy rollout evidence Job, a focused Codex App Server evidence Job, a focused MCP Gateway evidence Job, a focused eval/release evidence Job, and a focused finance evidence Job; it is documented and checked offline through `scripts/verify-stage2-evidence-k8s-manifests.sh`. `scripts/archive-stage2-production-evidence.sh` creates a temporary read-only Pod against the evidence PVC and streams a local tar.gz archive for release records. The opt-in Remote Computer pilot bundle now binds the Pod-mounted `mandoforge-remote-computer-state` PVC to the JuiceFS PV through `remote-computer-state-juicefs-pvc-patch.yaml`, while keeping KEDA and warm-pool manifests as pilot examples. Latest checks should be refreshed after this scheduler evidence slice. | Covered for Stage 2 skeleton; not a complete enterprise deployment. |

## Latest Verification Commands

Latest passing checks:

```bash
cargo fmt --all -- --check
node --check web/app.js
git diff --check
cargo test -p mandoforge-api
cargo check -p mandoforge-api --bin mandoforge-worker
cargo test -p mandoforge-api stage2_readiness -- --nocapture
ALLOW_BLOCKED=1 BASE_URL=http://127.0.0.1:8792 EVIDENCE_DIR=.mandoforge/stage2-production-evidence-local ./scripts/stage2-production-evidence-gate.sh
ALLOW_BLOCKED=1 BASE_URL=http://127.0.0.1:8792 SOURCE_EVIDENCE_DIR=.mandoforge/stage2-production-evidence-local ./scripts/stage2-completion-audit-gate.sh
./scripts/verify-static-ui-actionbook.sh
./scripts/verify-stage2-controller-env-template.sh
./scripts/verify-stage2-evidence-k8s-manifests.sh
./scripts/verify-stage2-evidence-archive.sh --self-test
./scripts/verify-observability-k8s-manifests.sh
./scripts/verify-remote-computer-k8s-manifests.sh
MANDOFORGE_K8S_NAMESPACE=agent-os ./scripts/archive-stage2-production-evidence.sh .mandoforge/stage2-production-evidence-YYYYMMDDTHHMMSSZ.tar.gz
ALLOW_STAGE2_CONTROLLER_PLACEHOLDERS=1 ./scripts/render-stage2-controller-secret.sh deploy/stage2-evidence/stage2-production-controllers.env.example >/tmp/mandoforge-stage2-controller-secret-render.yaml
kubectl kustomize deploy/k8s >/tmp/mandoforge-kustomize.out && wc -l /tmp/mandoforge-kustomize.out
kubectl kustomize deploy/stage2-evidence >/tmp/mandoforge-stage2-evidence-kustomize.out && wc -l /tmp/mandoforge-stage2-evidence-kustomize.out
kubectl kustomize deploy/stage2-production-evidence --load-restrictor LoadRestrictionsNone >/tmp/mandoforge-stage2-production-evidence-kustomize.out && wc -l /tmp/mandoforge-stage2-production-evidence-kustomize.out
kubectl kustomize deploy >/tmp/mandoforge-deploy-kustomize.out && wc -l /tmp/mandoforge-deploy-kustomize.out
lsof -nP -iTCP:8791 -sTCP:LISTEN || true
lsof -nP -iTCP:9324 -sTCP:LISTEN || true
```

Latest evidence:

```text
cargo test -p mandoforge-api: 211 passed
cargo test -p mandoforge-api stage2_readiness -- --nocapture: 1 passed
stage2 production evidence gate local inventory: stage2_status=blocked; open_gap_count=12; evidence_requirement_count=12; validation_declared_endpoint_count=26; validation_missing_endpoint_count=26
Stage 2 completion audit gate writes `.mandoforge/stage2-completion-audit/checklist.md` and `.mandoforge/stage2-completion-audit/checklist.json`, mapping every readiness `evidence_requirements[]` item to discovered readiness/validation endpoint artifacts and failing closed while readiness remains blocked or evidence artifacts are missing
MCP team-scoped evidence now auto-discovers the first active team through `/api/organizations` and `/api/organizations/{id}/teams` when `MANDOFORGE_STAGE2_TEAM_ID` is empty, writes `team-discovery.json`, and lets the completion audit gate resolve `{team_id}` endpoints from that artifact; local smoke with a temporary org/team resolved `unresolved_endpoint_count=0` while Stage 2 stayed blocked on missing production validation evidence
strict validation coverage gate fails closed without production validation evidence and reports missing endpoints; controller-backed validation mode reports missing controller configuration, for example POST /api/tenant-isolation/routing/validate returned HTTP 400 with MANDOFORGE_TENANT_ROUTING_CONTROLLER_URL required
static UI actionbook smoke ok
Remote Computer static Admin UI now exposes register computer, create lease, lease heartbeat/release/fail, and stale reclaim controls against the audited Remote Computer APIs; Actionbook verifies these lifecycle controls without Playwright/Chrome DevTools MCP
Stage 2 controller env template verifier ok; Stage 2 evidence K8s manifest verifier ok; Remote Computer K8s manifest verifier ok
kustomize deploy/k8s rendered 816 lines; kustomize deploy/stage2-evidence rendered 85 lines; kustomize deploy/stage2-production-evidence rendered 988 lines; kustomize deploy rendered 947 lines
Strict production controller Secret render fails closed on placeholder URLs and empty token/key values by default, keeps `MANDOFORGE_STAGE2_TEAM_ID` optional for MCP team auto-discovery, and renders the reviewed env contract when `ALLOW_STAGE2_CONTROLLER_PLACEHOLDERS=1` is set for template verification
Stage 2 controller drill assets now exist: `scripts/stage2-mock-controller.js`, `scripts/stage2-controller-drill.sh`, `scripts/stage2-controller-drill-live-gate.sh`, and `scripts/verify-stage2-controller-drill.sh`; the verifier checks route-specific mock statuses for validation, rollout, rollback, release, and finance controller responses, and the live gate starts a temporary API with mock controller URLs before running the full mock-controller production evidence path
Strict production evidence bundle includes persistent evidence PVC and production evidence Job with `/evidence` backed by `mandoforge-stage2-production-evidence`; the real controller Secret is rendered separately from the production env file
Production evidence PVC now has a standard read-only archive helper that streams a local tar.gz for release records after the strict Job completes and writes `.sha256` plus `.manifest.txt` verification sidecars; `scripts/verify-stage2-evidence-archive.sh --self-test` validates the archive verification path
Remote Computer pilot bundle now binds the Pod-mounted `mandoforge-remote-computer-state` PVC to the JuiceFS PV through `remote-computer-state-juicefs-pvc-patch.yaml` instead of rendering only a separate reference PVC
GitHub Actions run 25830961757 passed the expanded CI gate on commit 3f77b5969fcfa439110980da652f132a86e829a4: fmt, check, test, node syntax, Stage 1/2 script syntax, controller env verifier, Stage 2 evidence K8s verifier, Remote Computer K8s verifier, K8s/evidence overlay render, and strict production evidence bundle render
ports 8791 and 9324 had no residual listeners after Actionbook verification
```

## Open Completion Gaps

These gaps prevent Stage 2 from being marked complete:

1. Full production cross-tenant runtime isolation beyond the default tenant boundary; current readiness blocks production routing until runtime tenant routing, full RLS readiness, and optional required external routing-controller evidence exist.
2. Production multi-step rollout orchestration beyond preview/history/due-run/rollback surfaces; policy rollout orchestration now has readiness, validation, optional required external controller evidence, and UI triggering, but real production rollout supervision still needs a real target.
3. Production provider policy gate workflow beyond current lifecycle approval, summary, audited deployment validation, optional required external deployment-controller evidence, manual/scheduled gate run history, report surfaces, external rollout controller execution, and external rollback/recovery controller execution.
4. Production secret value storage and real external KMS/HSM rotation/recovery execution beyond the Vault production rotation and recovery readiness gates.
5. Production load-validated autoscaling, isolated worker-pool operations, and real-cluster Remote Computer state-sync / sidecar replacement validation beyond the existing Rust worker binary, hardened K8s worker manifests, HPA/KEDA manifests, worker production-ops gate, env-gated recovery run, optional required worker load-validation controller, optional/required state-sync and sidecar validation controllers, and broker-backed Redis/Core NATS/JetStream handoff.
6. Production approval notification operations beyond current webhook/Slack/email-relay delivery, persisted channel policy, bounded retries/backoff, audited delivery runs, routing readiness summary, deployment/ops validation gates, and optional required external deployment/ops controller evidence against real notification providers.
7. Production MCP connector rollout orchestration beyond current summary, audited due-run history, production orchestration gate, deployment-validation gate, external deployment/rollout/rollback controllers, required-controller gates, manual apply, and rollback.
8. Production Codex App Server control-plane operations beyond current steering, polling, traces, artifact sync, summary, stale-poll supervision, production-ops readiness, audited deployment/ops validation gates, and optional required external deployment/ops controller evidence.
9. Production multi-step eval/release rollout orchestration beyond current approval, due-run, summary, rollback, production rollout readiness, production orchestration gate, external rollout/orchestration/deployment/rollback controller execution, required-controller gates, and real production-target rollout validation.
10. Real-cluster collector rollout proof beyond the current readiness, production-ops gate, audited collector deployment-validation gate, audited collector cluster rollout gate, optional external collector deployment/cluster/remediation controller execution, and required-controller supervision gates.
11. Production finance operations beyond current readiness, bounded close run, production close gate, alerts, forecast, audit-evidence, export surfaces, external close/reconciliation controller execution, required-controller gates, and real accounting-system reconciliation against a production target.
12. Full production UI CRUD flows and dashboard polish.

## Completion Decision

Stage 2 is not complete as of this audit. The repo has substantial governed-runtime capability, but the remaining Partial and Gap items above must be either implemented or explicitly moved out of the Stage 2 success criteria before the goal can be marked complete.
